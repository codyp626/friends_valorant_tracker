@page "/rank"
@using FriendsTracker.Components.Pages;

<button class="btn btn-primary" @onclick="@updateMongoRanks">Update Player Data</button>
<div class="card_container">
    <div class="card">
        Ranks
        @foreach (var player in rankList)
        {
            <div class="player">
                <p class="player_name">@player.Data.Name</p>
                <img class="rank_icon" src=@player.Data.CurrentData.Images.Small.ToString()>
                <p class="rr_number">@player.Data.CurrentData.RankingInTier RR</p>
            </div>
        }
    </div>
    <div class="card">
        Card 2
    </div>
    <div class="card">
        Card 3
    </div>
</div>